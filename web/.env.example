# Reverb (realtime) — must match the backend's REVERB_APP_KEY / REVERB_HOST / REVERB_PORT.
# The browser connects to Reverb directly (not proxied through /api).
VITE_REVERB_APP_KEY=
VITE_REVERB_HOST=localhost
VITE_REVERB_PORT=8080
VITE_REVERB_SCHEME=http
